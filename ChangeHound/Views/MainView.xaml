<Window x:Class="ChangeHound.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:local="clr-namespace:ChangeHound"
        xmlns:vm="clr-namespace:ChangeHound.ViewModels"
        mc:Ignorable="d"
        Title="ChangeHound" Height="450" Width="800"
        ui:WindowHelper.UseModernWindowStyle="True"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding FileChanges}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Event" DisplayMemberBinding="{Binding EventType}" Width="100"/>
                    <GridViewColumn Header="File Path" DisplayMemberBinding="{Binding FilePath}" Width="400"/>
                    <GridViewColumn Header="Timestamp" DisplayMemberBinding="{Binding Timestamp, StringFormat='HH:mm:ss'}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
